# ==========================================================
#  THIS FILE IS MANAGED BY CHEZMOI
#
#  Do NOT edit this file directly.
#
#  Common commands:
#    Edit source:    chezmoi edit ~/.zshrc
#    View changes:  chezmoi diff
#    Apply changes: chezmoi apply
# ==========================================================

# Protect against this file being sourced by a non-interactive shell.
[[ -o interactive ]] || return

# Load managed modules in filename order.
for _zsh_module in \
  "${XDG_CONFIG_HOME:-$HOME/.config}/zsh/modules/"*.zsh(.N); do
  source "$_zsh_module"
done

unset _zsh_module
